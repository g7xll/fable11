import DemoOne from "@/components/ui/demo";

/**
 * The app is the integration's home page: it simply renders the prompt's
 * demo (`DemoOne`), which mounts the `hero-ascii` <Home /> full-bleed — the
 * "best place to use this component" being a full-viewport landing hero.
 */
export default function App() {
	return <DemoOne />;
}
